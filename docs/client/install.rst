Installation
============
The Webcandy Client can currently be found on
`Github <https://github.com/gcpreston/webcandy-client>`_. Clone the repository
and run :code:`pip install .` from the root directory.